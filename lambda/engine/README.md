# Engine Core

This folder contains the core orchestration logic of the Lambda Engine. It includes configuration loading, logging, database client, and AI processor modules as defined in the PRD. The engine invokes platform adapters and coordinates all processing flows.
