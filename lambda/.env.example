# Environment variables for local development of the Lambda Engine
# For local development, copy this file to .env and fill in the values.
# In staging/production, these values are typically managed by AWS Secrets Manager and 
# made available as environment variables to the Lambda function, or fetched by config.ts.

# Core Application Configuration
# NODE_ENV: The runtime environment (development, production, test). Affects secret loading.
NODE_ENV=development

# LOG_LEVEL: The minimum level of logs to output (e.g., debug, info, warn, error).
LOG_LEVEL=debug

# Supabase Configuration (required for dbClient.ts and AppConfig)
SUPABASE_URL="your_supabase_project_url_here"
SUPABASE_SERVICE_ROLE_KEY="your_supabase_service_role_key_here"

# OpenAI Configuration (required for AppConfig, typically used by aiProcessor.ts)
OPENAI_API_KEY="your_openai_api_key_here"

# --- Platform Adapter Configurations (Examples) ---
# These are examples. Add or remove sections based on the adapters you are using.

# Discourse Adapter Configuration
DISCOURSE_API_URL="your_discourse_api_url_here"
DISCOURSE_API_KEY="your_discourse_api_key_here"

# Add other platform-specific API keys and URLs as needed, for example:
# TWITTER_BEARER_TOKEN="your_twitter_bearer_token_here"
# DISCORD_BOT_TOKEN="your_discord_bot_token_here"
