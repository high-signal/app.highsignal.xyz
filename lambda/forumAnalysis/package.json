{
  "name": "discourse-forum-analysis-lambda",
  "version": "1.0.0",
  "description": "Lambda function for forum user analysis",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "package": "mkdir -p bundle && rsync -a . bundle/ --exclude bundle --exclude discourse-forum-analysis.zip --exclude .git --exclude node_modules && rsync -a node_modules/ bundle/node_modules/ && cd bundle && zip -r ../discourse-forum-analysis.zip . && cd .. && rm -rf bundle"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.3",
    "axios": "^1.8.4",
    "dotenv": "^16.4.5",
    "openai": "^4.95.1"
  }
}