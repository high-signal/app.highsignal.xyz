# Shared Utilities

This folder is for code, types, and utilities that are shared across multiple adapters or the engine core. Place common logic here to avoid duplication. (Per PRD, this may include helpers, types, or validation logic used by more than one module.)
