{
  "name": "high-signal-lambda",
  "version": "1.0.0",
  "description": "High Signal Lambda functions",
  "author": {
    "name": "Eridian",
    "url": "https://eridian.xyz"
  },
  "license": "Proprietary",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "package": "mkdir -p bundle && rsync -a . bundle/ --exclude bundle --exclude lambda-code.zip --exclude .git --exclude node_modules && rsync -a node_modules/ bundle/node_modules/ && cd bundle && zip -r ../lambda-code.zip . && cd .. && rm -rf bundle"
  },
  "dependencies": {
    "@aws-sdk/client-cloudwatch-logs": "^3.901.0",
    "@aws-sdk/client-lambda": "^3.851.0",
    "@supabase/supabase-js": "^2.51.0",
    "axios": "^1.8.4",
    "dotenv": "^16.4.5",
    "expr-eval": "^2.0.2",
    "openai": "^4.95.1",
    "out-of-character": "^2.0.1",
    "pg": "^8.16.3"
  }
}
